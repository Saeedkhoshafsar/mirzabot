# 🧭 درک من از پروژه + چشم‌انداز آینده

> این فایل تصویر ذهنی ایجنت (AI Developer) از پروژه است.
> اگر جایی با تصور شما فرق دارد، همان نقطه‌ی سوءتفاهم است — بگویید تا اصلاح شود.

---

## ⚡️ حالت فعال توسعه — بخوان، بعد کد بزن! (ACTIVE DEVELOPMENT MODE)

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                                                 ┃
┃   🎯  PANEL_MODE فعلی توسعه:  « shop »            ┃
┃       (آنلاین‌شاپ / فروشگاه عمومی)                ┃
┃                                                 ┃
┃   هر تسک جدید، مگر اینکه صریحاً خلافش گفته    ┃
┃   شود، برای حالت shop طراحی، پیاده و تست       ┃
┃   می‌شود. VPN فقط «میراث محافظت‌شده» است،     ┃
┃   نه مسیر توسعه.                                ┃
┃                                                 ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

### چرا این بخش اینجاست؟

این پروژه **ظاهرش** یک ربات فروش VPN است (چون از آنجا شروع شده)، اما **باطنش**
یک پلتفرم چندمحصولی است که با متغیر `PANEL_MODE` هنگام دیپلوی (Coolify) تعیین
می‌شود هر کانتینر چه محصولی باشد:

| `PANEL_MODE` | محصول | وضعیت توسعه |
|---|---|---|
| `shop` | 🛒 آنلاین‌شاپ (کالای فیزیکی/عمومی) | ✅ **تمرکز فعلی — همه‌ی توسعه‌ها اینجاست** |
| `digital` | 💾 محصولات دیجیتال (لایسنس/کد) | 🕜 بعد از تکمیل shop |
| `channel` | 📁 کانال/اشتراک محتوا | 🕜 بعد از تکمیل shop |
| `custom` | ⚙️ سفارشی (Flow Editor) | 🕜 بعد از تکمیل shop |
| `vpn` | 🔐 فروش VPN | ❄️ **فریزشده** — فقط حفظ بدون رگرسیون، هیچ فیچر جدیدی برایش ساخته نمی‌شود |

### قواعد الزامی برای هر جلسه‌ی توسعه (چک‌لیست ایجنت)

1. **قبل از هر کاری** این بخش و بنر بالای PLAN.md را بخوان — حالت فعال = مسیر توسعه.
2. وقتی کاربر می‌گوید «دکمه‌ی خرید»، «محصول»، «سفارش»، «مشتری» — منظورش دنیای
   **فروشگاه (shop)** است، نه سرویس/پلن VPN. هرگز برعکس تفسیر نکن.
3. هر فیچر جدید باید در حالت `PANEL_MODE=shop` تست شود (نه حالت پیش‌فرض vpn).
4. لایه‌ی VPN فقط در حد «صفر رگرسیون» محافظت می‌شود؛ ارجاع به کدهای VPN برای
   الگوبرداری اشکالی ندارد، اما خروجی کار همیشه برای فروشگاه است.
5. اگر تمرکز پروژه عوض شد (مثلاً رفتیم سراغ `digital`)، **همین بخش + بنر PLAN.md**
   باید در همان PR به‌روز شوند. این دو فایل «تک‌منبع حقیقت» تمرکز پروژه‌اند.

### زنجیره‌ی تحویل PANEL_MODE (از Coolify تا کد)

```
Coolify ENV panel ──▶ docker-compose.yml (باید PANEL_MODE را لیست کند!)
                       ──▶ کانتینر ──▶ entrypoint.sh (اعتبارسنجی + تزریق در config.php)
                            ──▶ panel_mode() در function.php (ENV ⏵ botsaz ⏵ setting ⏵ vpn)
```

⚠️ درس تلخ: تا قبل از این اصلاح، `PANEL_MODE` در docker-compose لیست نشده بود؛ هرچه
در Coolify ست می‌شد هیچ‌وقت به کانتینر نمی‌رسید و ربات بی‌سروصدا به حالت vpn برمی‌گشت
(برای همین دکمه‌های فروشگاه دیده نمی‌شد). الان زنجیره کامل و قطعی است.

---

## ۱) این پروژه چیست؟ (یک جمله)

**میرزابات: یک پلتفرم چندمحصولی ربات‌ساز تلگرامی + پنل مدیریت وب، که با متغیر `PANEL_MODE` هنگام دیپلوی تعیین می‌شود چه محصولی باشد (فروشگاه، دیجیتال، کانال، سفارشی، VPN) — و تمرکز فعلی توسعه، ساخت یک آنلاین‌شاپ بی‌نقص در حالت `shop` است.**

```
گذشته (میراث)             حال (تمرکز فعلی)               آینده
─────────────             ────────────────              ─────────
ربات فروش VPN     →    🎯 PANEL_MODE=shop        →    فروشگاه‌ساز چندمستاجری
(فقط نگهداری)            آنلاین‌شاپ بی‌نقص              (هر کسب‌وکار، هر قالب)
```

---

## ۲) معماری فعلی — نقشه‌ی درختی

```
mirzabot/
│
├── 🤖 هسته‌ی ربات تلگرام (stateless webhook)
│   ├── index.php          ← نقطه‌ی ورود وب‌هوک؛ دیسپچر همه‌ی پیام‌ها و callbackها
│   │                         • گیت فروشگاه: الگوهای shop*/cart* فقط اینجا فعال می‌شوند
│   │                         • مسیریابی «خرید» بر اساس panel_mode (vpn ↔ shop)
│   ├── function.php       ← مغز ربات؛ همه‌ی منطق فروشگاه و VPN
│   │                         • منوی فروشگاه، سبد خرید، پروفایل، دسته‌بندی، جستجو
│   │                         • panel_mode() / bot_label() / php_database_dump()
│   ├── keyboard.php       ← کیبوردهای ثابت (منوی اصلی، پنل ادمین)
│   ├── botapi.php         ← لایه‌ی ارتباط با Telegram Bot API
│   ├── table.php          ← مایگریشن خودکار جداول دیتابیس
│   ├── jdf.php            ← تاریخ شمسی
│   └── lang/              ← متن‌های فارسی (textbotlang)
│
├── 🖥 پنل مدیریت تحت وب (panel/)
│   ├── store.php / product.php      ← مدیریت فروشگاه و محصولات (+ تنوع/پس‌کد)
│   ├── orders.php / reports.php     ← سفارش‌ها و گزارش‌ها
│   ├── categories.php               ← دسته‌بندی‌ها
│   ├── discounts.php / shipping.php ← تخفیف و حمل‌ونقل
│   ├── variant_schemas.php          ← الگوهای تنوع محصول (رنگ/سایز/…)
│   ├── labels.php                   ← بازنویسی متن دکمه‌ها (botlabels)
│   ├── flow.php                     ← ✨ ویرایشگر بصری فلوی دکمه‌ها (Visual Flow)
│   └── automation.php               ← اتوماسیون و وب‌هوک n8n (با امضای HMAC)
│
├── 🔌 API (api/)                    ← اندپوینت‌های REST برای پنل و سرویس‌های بیرونی
│
├── 💳 پرداخت (payment/)             ← زرین‌پال، آقای پرداخت، NowPayments، کارت‌به‌کارت
│                                       همه به کیف پول (Balance) واریز می‌کنند
│
├── 🤝 چندمستاجری / ربات‌ساز (vpnbot/)
│   └── {id}{username}/index.php    ← هر کاربر می‌تواند ربات فرزند خودش را بسازد
│                                       (جدول botsaz + تنظیمات JSON اختصاصی هر ربات)
│
├── ⏰ کرون‌جاب‌ها (cronbot/)
│   └── backupbot.php               ← بکاپ خودکار → ارسال به تلگرام
│                                       (mysqldump؛ اگر خطا داد → دامپ PHP خالص ♻️)
│
├── 🧬 لایه‌ی VPN (میراث پروژه)      ← Marzban / Hiddify / x-ui / WGDashboard / mikrotik
│                                       دست‌نخورده و ایزوله؛ فقط در حالت vpn فعال است
│
└── 📚 مستندات                       ← PLAN.md (نقشه‌ی راه) + docs/store-guide.md
```

---

## ۳) مدل داده — جداول کلیدی

| جدول | نقش | نکته‌ی مهم |
|---|---|---|
| `user` | کاربر ربات | `number` (تلفن تأییدشده)، `Balance` (کیف پول)، `shop_cart` (سبد JSON) |
| `product` | محصول | `attributes` JSON شامل تنوع‌ها (پس‌کد)، تصاویر، موجودی؛ `category` به‌صورت CSV |
| `category` | دسته‌بندی | نام فارسی → هش ۸ کاراکتری در callback (محدودیت ۶۴ بایت تلگرام) |
| `Payment_report` | سفارش/تراکنش | تاریخچه‌ی خرید و «سفارش‌های من» |
| `customer_address` | آدرس‌های مشتری | ویزارد ۶ مرحله‌ای: نام→تلفن→استان→شهر→آدرس→کدپستی |
| `botsaz` | ربات‌های فرزند | ستون `setting` JSON = تنظیمات اختصاصی هر ربات (از جمله panel_mode) |
| `discount` | کد تخفیف | اعمال در تسویه |
| `botlabels` | متن سفارشی دکمه‌ها | از طریق `bot_label()` |

---

## ۴) سفر مشتری در فروشگاه (وضعیت فعلی — پیاده‌سازی‌شده ✅)

```
/start یا دکمه‌ی «خرید» (در حالت فروشگاه)
   │
   ▼
🏬 منوی اصلی فروشگاه (shopmenu)
   ├── 🛍 خرید ──────────────┐
   │      ├── 🆕 جدیدترین محصولات   ← ۵تایی صفحه‌بندی با «بعدی/قبلی»
   │      ├── 🗂 دسته‌بندی‌ها         ← هر دسته ۵تایی صفحه‌بندی
   │      └── 🔍 جستجوی پیشرفته
   │             │
   │             ▼
   │      📦 کارت محصول
   │      ├── 🖼 «مشاهدهٔ همهٔ تصاویر و تنوع‌های این محصول» (دکمه‌ی شیشه‌ای)
   │      ├── ➕ افزودن به سبد        ← برای همه آزاد (بدون گیت)
   │      └── 🛒 خرید مستقیم         ← 🔒 گیت پروفایل
   │
   ├── 👤 پروفایل
   │      ├── 📱 ثبت/تغییر شماره (با request_contact تأییدشده)
   │      └── 📍 افزودن آدرس (ویزارد ۶ مرحله‌ای)
   │
   ├── 🛒 سبد خرید (با شمارنده)
   │      └── تسویه ← 🔒 گیت پروفایل («سبد شما محفوظ می‌ماند»)
   │
   ├── 📦 سفارش‌های من
   └── 🎧 پشتیبان

🔒 گیت پروفایل: شماره ثبت نشده؟ → هدایت به تکمیل پروفایل (خرید قطع نمی‌شود)
💰 موجودی کم؟ → دکمه‌ی «💳 افزایش موجودی» → درگاه‌های پرداخت → برگشت به خرید
🚚 تحویل بر اساس نوع محصول: فیزیکی=آدرس+ارسال / دیجیتال=تحویل آنی / فایل=ارسال فایل
```

---

## ۵) اصول طراحی که رعایت می‌کنم (قواعد نانوشته‌ی پروژه)

0. **حالت فعال اول — همیشه** — پیش از هر تغییر، بخش «⚡️ حالت فعال توسعه» بالای همین فایل را بخوان؛ توسعه برای همان حالت انجام و در همان حالت تست می‌شود (الان: `shop`).
1. **صفر رگرسیون VPN** — همه‌ی منطق فروشگاه پشت الگوهای `shop*`/`cart*` و گارد `panel_mode() !== 'vpn'` است؛ ربات‌های VPN فعلی هیچ تغییری حس نمی‌کنند — اما این «محافظت» است، نه «توسعه».
2. **Stateless بودن وب‌هوک** — وضعیت چندمرحله‌ای فقط در `step` و `Processing_value` نگه‌داری می‌شود.
3. **سازگاری با ربات‌ساز** — هر قابلیت جدید باید هم در ربات اصلی و هم در ربات‌های فرزند (botsaz) کار کند.
4. **دو دنیای موازی: پنل وب + پنل داخل تلگرام** — هر قابلیت فروشگاهی باید از هر دو قابل مدیریت باشد.
5. **PLAN.md محور** — هر مرحله ۴–۶ تسک، تست، کامیت، PR، گزارش ۳ خطی.
6. **تحویل تا انتهای زنجیره** — فیچری که در کد هست ولی در دیپلوی دیده نمی‌شود، تحویل نشده؛ زنجیره‌ی ENV → compose → entrypoint → PHP باید برای هر تنظیم جدید کامل باشد.

---

## ۶) 🔮 چشم‌انداز آینده — «بهترین حالتی که این پروژه می‌تواند بشود»

> تصویر نهایی: **یک پلتفرم SaaS فروشگاه‌ساز تلگرامی فارسی** — مثل «شاپیفای، اما داخل تلگرام» — که هر کسی در ۵ دقیقه ربات فروشگاه اختصاصی‌اش را بالا می‌آورد.

```
🏆 میرزا‌شاپ (تصویر نهایی)
│
├── 🎨 ۱. قالب‌های آماده‌ی کسب‌وکار (Template Marketplace)
│   ├── 🛒 فروشگاه فیزیکی   (پوشاک، لوازم، سوپرمارکت)
│   ├── 💾 محصولات دیجیتال  (لایسنس، اشتراک، کد هدیه)
│   ├── 📁 فروش فایل        (دوره، کتاب، موزیک)
│   ├── 🔐 VPN/اشتراک       (همان میراث فعلی، به‌عنوان یک قالب)
│   └── ⚙️ سفارشی           (ساخته‌شده با Flow Editor)
│        → کاربر موقع ساخت ربات، قالب را انتخاب می‌کند و منو/متن/فلو آماده می‌شود
│
├── 🛍 ۲. تجربه‌ی خرید بی‌نقص
│   ├── انتخاب تنوع (رنگ/سایز) هنگام افزودن به سبد ← قدم بعدی نقشه‌ی راه
│   ├── پرداخت مستقیم درگاهی در تسویه (بدون اجبار به شارژ کیف پول)
│   ├── پیگیری مرسوله (اتصال به API پست/تیپاکس)
│   ├── نظرات و امتیاز محصول
│   └── اعلان هوشمند: «موجود شد»، «تخفیف خورد»، سبد رهاشده
│
├── 🖥 ۳. پنل مدیریت حرفه‌ای
│   ├── داشبورد فروش زنده (نمودار، پرفروش‌ها، نرخ تبدیل)
│   ├── مدیریت انبار و هشدار اتمام موجودی
│   ├── CRM سبک: پروفایل مشتری، تاریخچه، برچسب‌گذاری
│   └── خروجی اکسل/حسابداری
│
├── 🔗 ۴. اکوسیستم اتوماسیون
│   ├── n8n / وب‌هوک امضاشده برای هر رویداد (سفارش، پرداخت، ثبت‌نام)
│   ├── API عمومی مستندشده برای توسعه‌دهندگان
│   └── اتصال به پیامک، ایمیل، گوگل‌شیت
│
├── 🤝 ۵. SaaS چندمستاجری واقعی
│   ├── پلن‌های اشتراکی برای صاحبان فروشگاه (رایگان/حرفه‌ای)
│   ├── دامنه و برندینگ اختصاصی هر فروشگاه
│   ├── ایزولاسیون کامل داده‌ی هر مستاجر
│   └── بازار افزونه (درگاه‌ها، روش‌های ارسال، قالب‌ها)
│
└── 🛡 ۶. زیرساخت قابل‌اتکا
    ├── بکاپ خودکار چندلایه (الان هست ✅ — با fallback خالص PHP)
    ├── نصب یک‌کلیکی (install.sh / Docker — الان هست ✅)
    ├── تست‌های خودکار برای هر مرحله (روال فعلی ✅)
    └── ارتقای بدون قطعی برای همه‌ی ربات‌های فرزند
```

### نقشه‌ی راه از «الان» تا «آنجا» (ترتیب پیشنهادی)

| فاز | محتوا | وضعیت |
|---|---|---|
| ✅ فاز ۱ | فروشگاه پایه: محصول، سبد، تسویه، آدرس، تخفیف | انجام شد (مراحل ۱–۱۴) |
| ✅ فاز ۲ | منوی فروشگاه، دسته‌بندی، جدیدترین‌ها، گیت پروفایل، کیف پول | انجام شد (مراحل ۱۵–۱۶) |
| ⏳ فاز ۳ | تنوع محصول در سبد + خلاصه سفارش، پرداخت مستقیم در تسویه | قدم بعدی |
| 🔜 فاز ۴ | قالب‌های آماده هنگام ساخت ربات فرزند + داشبورد فروش | میان‌مدت |
| 🔮 فاز ۵ | نظرات، پیگیری مرسوله، اعلان هوشمند، پلن‌های SaaS | بلندمدت |

---

## ۷) اگر جایی را اشتباه فهمیده‌ام…

محتمل‌ترین نقاط سوءتفاهم که خوب است تأیید/رد کنید:

- [ ] آیا هدف نهایی «فروشگاه‌ساز برای دیگران» است، یا فقط «یک فروشگاه برای خود شما»؟ (تصور من: هر دو — اول فروشگاه بی‌نقص، بعد قالب‌پذیر برای ربات‌ساز)
- [ ] آیا بخش VPN باید در آینده هم حفظ شود؟ (تصور من: بله، به‌عنوان یکی از قالب‌ها، بدون حذف)
- [ ] آیا پرداخت باید کیف‌پول‌محور بماند یا درگاه مستقیم هم اضافه شود؟ (تصور من: هر دو، با اولویت تجربه‌ی روان)
